import { PostalCodeValidationFactory } from './postal-code-validation-factory';

describe('PostalCodeValidationFactory', () => {
  it('should create an instance', () => {
    expect(new PostalCodeValidationFactory()).toBeTruthy();
  });
});
