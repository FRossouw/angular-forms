import { StreetValidationFactory } from './street-validation-factory';

describe('StreetValidationFactory', () => {
  it('should create an instance', () => {
    expect(new StreetValidationFactory()).toBeTruthy();
  });
});
