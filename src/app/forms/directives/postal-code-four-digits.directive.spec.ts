import { PostalCodeFourDigitsDirective } from './postal-code-four-digits.directive';

describe('PostalCodeFourDigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new PostalCodeFourDigitsDirective();
    expect(directive).toBeTruthy();
  });
});
