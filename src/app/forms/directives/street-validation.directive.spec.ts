import { StreetValidationDirective } from './street-validation.directive';

describe('StreetValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new StreetValidationDirective();
    expect(directive).toBeTruthy();
  });
});
