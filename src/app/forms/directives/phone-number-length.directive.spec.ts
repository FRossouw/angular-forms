import { PhoneNumberLengthDirective } from './phone-number-length.directive';

describe('PhoneNumberLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneNumberLengthDirective();
    expect(directive).toBeTruthy();
  });
});
