import { LocalPhoneNumberDirective } from './local-phone-number.directive';

describe('LocalPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
